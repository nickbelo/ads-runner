Feature updates:

1. Slide scheduling — set time windows per slide (e.g. a promo only runs 9am–12pm)                              *backlog
2. Playlist groups — organize slides into named campaigns and switch between them                               *backlog
3. Remote deploy trigger — a one-click deploy button in the admin panel that pulls from GitHub                  *done
4. Slide duration per video — auto-detect video length using ffprobe so you don't have to set it manually       *done
5. Screen on/off control — use HDMI CEC to turn the LG TV on/off on a schedule directly from the RPi            *pending
6. Multi-display support — if you ever add a second RPi and TV                                                  *pending
7. Authentication on the admin panel — basic login so the upload page isn't open to everyone on the network     *done
8. Arranging slides in Admin Page                                                                               *pending

Bugs fixes:

1. Disable / Remove mouse pointer
2. Enable sounds in video slides